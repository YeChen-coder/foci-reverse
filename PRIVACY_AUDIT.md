# 公开发布隐私审计

本报告对应当前公开副本。审计目的不是证明私有协议的每一个未知字节都没有含义，
而是确认已知的身份信息、凭据、手机数据和无关蓝牙流量均未进入发布包，并明确说明
剩余的数据风险。

## 结论

本副本可以作为去标识化的 FOCI 逆向工程研究资料公开。最终扫描结果：

| 检查项 | 结果 |
| --- | ---: |
| 真实设备 UID（二进制大小端及文本形式） | 0 命中 |
| 真实写入密钥（二进制大小端及文本形式） | 0 命中 |
| 真实设备蓝牙地址（正序、逆序及文本形式） | 0 命中 |
| 已知手机序列号 | 0 命中 |
| 本机用户名、用户目录和私有项目绝对路径 | 0 命中 |
| 电子邮件地址模式（压缩 CSV 解压后检查） | 0 命中 |
| APK、DEX、数据库、原始 bugreport/logcat、真实本地配置 | 0 个文件 |
| 无关 HCI 包类型或非 ATT L2CAP CID | 0 个 |

## 对抓包做了什么

原始 Android HCI 日志没有放入本目录。公开的两个 `.btsnoop` 文件由原始日志重新构造，
只保留能够归属于 FOCI 会话窗口的 ATT PDU，并进行了以下转换：

1. 删除 HCI Event、SMP 配对、扫描、广播、控制器信息和所有非 ATT L2CAP 流量。
2. 删除不属于目标 FOCI 会话时间窗或连接句柄的 ATT 流量。
3. 将控制器连接句柄重新编号。
4. 将 btsnoop 绝对时间统一重定基到 `2025-01-01 00:00:00 UTC`，保留包之间的相对时序。
5. 将已知应用层时间字段同步重定基，并把配置中的时区偏移置零。
6. 用固定的合成 UID、合成设备地址和合成写入密钥替换真实值。
7. 将每个完整 ATT PDU规范化为一个 H4 ACL 记录。因此 ATT 的顺序和内容仍可分析，
   但原控制器层的 ACL 分片边界不再保留。

公开抓包的结构校验结果：

| 文件 | H4 记录 / ATT PDU | 可解码 FOCI 帧 | H4 包类型 | L2CAP CID |
| --- | ---: | ---: | --- | --- |
| `capture_01_ring_buffer.btsnoop` | 43,186 | 7,267 | ACL Data (`0x02`) | ATT (`0x0004`) |
| `capture_02_rotation_continuation.btsnoop` | 6,117 | 1,006 | ACL Data (`0x02`) | ATT (`0x0004`) |

## 没有包含什么

- 原始 Android bugreport、完整 HCI snoop 文件和 logcat；
- 手机界面截图、UI XML、手机型号/运营商/账户信息；
- FOCI 官方 APK、DEX 或反编译产物；
- 真实 `foci.local.json`、真实 UID、写入密钥和蓝牙地址；
- Python 虚拟环境、缓存、测试缓存和编译字节码；
- 本机路径和 Git 历史。

## 扫描方法

审计同时覆盖文本和二进制：

- 使用私有本地配置作为比对源，但私有值没有写入任何审计脚本或报告；
- 在每一个发布文件中查找真实 UID 与写入密钥的大小端字节序；
- 查找真实设备地址的正序/逆序字节及常见文本写法；
- 查找已知手机标识、本机用户名、绝对用户目录、电子邮件和电话号码模式；
- 解压 `.csv.gz` 后再做文本模式检查，避免把压缩数据的随机字节误判为文本；
- 枚举文件类型和文件名，排除 APK、DEX、数据库、原始系统日志与真实本地配置；
- 解析 btsnoop 记录，确认只含 ACL Data，并解析 L2CAP 头确认只含 ATT CID；
- 用公开分析器重新组帧，并逐行解析 JSONL、CSV 与压缩 CSV。

## 仍然保留的数据及风险

公开数据有意保留了理解协议所需的 FOCI 行为遥测，包括状态标签、专注/平静相关数值、
信号质量、提醒配置切换和相对时间。因此，它不是“空白样例”，而是一段已经去身份化的
真实使用轨迹。别人可以观察到这段采集期间状态值怎样变化，但不能从已知字段直接获得
设备或手机身份。

由于 FOCI 是未公开的私有协议，无法从数学上证明每个尚未命名的 payload 字节都绝不
编码某种设备特征。当前处理已经替换所有已识别的身份与凭据字段，并移除了无关手机流量；
发布者仍应接受“去标识化行为遥测仍具有一定关联风险”这一通常适用于真实数据集的限制。

## 可复核性

脱敏实现位于 `tools/sanitize_foci_btsnoop.py`。协议解析器、数据导出脚本、测试和
`DATA_INVENTORY.md` 中的 SHA-256 可以用于独立复核。若以后重新从私有原始日志生成发布
数据，应再次执行同等级别的扫描，不能把本报告自动视为适用于新文件。
